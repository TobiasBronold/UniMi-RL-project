# VFA-1: Navigating in the Fog

Reinforcement Learning project — comparison of tabular Q-learning and linear function approximation in a partially observable gridworld environment.

## Setup
```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

## Run
```bash
python main.py
```